scenedetect==0.7
ultralytics==8.4.46
# CPU-only torch/torchvision: container has no GPU, so the CUDA wheels would waste
# ~700MB+ of disk and time downloading nvidia_cudnn_cu13 etc.
# Use `--extra-index-url` (not `--index-url`) so PyPI is also searched for all the
# non-torch packages below (fastapi, mediapipe, etc.).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0
torchvision==0.26.0
tqdm==4.67.3
yt-dlp
faster-whisper==1.2.1
google-genai==1.75.0
python-dotenv==1.2.2
mediapipe==0.10.14
boto3==1.43.4

fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.27
httpx==0.28.1
Pillow==12.2.0
beautifulsoup4==4.14.3

# LLM routing (replaces direct minimax_client calls)
litellm

# Free TTS fallback (Microsoft Edge TTS, 400+ voices)
edge-tts

# Trend research (Google Trends)
pytrends

# Video editing (wraps FFmpeg with Python API)
mcp-video

# Audio processing
pydub
soundfile

# Image processing
opencv-python-headless
